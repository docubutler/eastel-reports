-- Query 1: Active Subscribers TODO placeholder

/*
TODO:
- Replace this placeholder with the real active-subscriber query.
- Current repo context does not show the source table or business rule for active subscribers.
*/
SELECT
    CAST(NULL AS text) AS service_type,
    CAST(NULL AS text) AS charge_type,
    CAST(NULL AS text) AS count_value
WHERE FALSE;
